package cl.thisisalexis.common.core.api;

/**
 * A general descriptor for other DocumentedApi interfaces which are implemented by classes that acts as aa API
 * method container which need to be documented using annotations; the main idea behind it is to take documentation
 * out of the API implementation classes and put them in a different abstraction layer
 *
 * @autor Alexis Bravo
 */
public interface DocumentedApi {
}
