package cl.thisisalexis.common.core.api;

/**
 * An interface to generalize all API controller container classes, it means, those that acts as an API layer container
 *
 * @author Alexis Bravo
 */
public interface ApiControllerInt {
}
